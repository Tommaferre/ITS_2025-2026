let radius = 5; // Change this value manually

let circumference = 2 * Math.PI * radius; // Math.PI is a built-in JavaScript constant (≈ 3.14159)

let area = Math.PI * Math.pow(radius, 2);

console.log(`The circumference is: ${circumference.toFixed(2)}`);// toFixed() is a built-in JavaScript method that formats a number to a fixed number of decimal places
console.log(`The area is: ${area.toFixed(2)}`);//${} is a template literal that allows you to embed expressions in a string
